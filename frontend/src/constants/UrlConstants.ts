const apiBaseUrl: string = "http://localhost:8000/api"

const UrlConstants: object = {
  apiLoginUrl: `${apiBaseUrl}/login`,
  apiBaseUserUrl: `${apiBaseUrl}/user`,
  apiAdminUrl: `${apiBaseUrl}/admin/timestamps`,
}

export default UrlConstants
